[build]
  command = "node -e \"const fs=require('fs');const url=process.env.SUPABASE_URL;const key=process.env.SUPABASE_ANON_KEY;fs.writeFileSync('supabase.config.js','export const SUPABASE_CONFIG = { url: \\\\\"'+url+'\\\\\", anonKey: \\\\\"'+key+'\\\\\" };');\" && npm run build"
  publish = "."
